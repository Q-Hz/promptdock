# 按 GitHub 数字用户 ID 归档日志

> 2026-09-05 清理说明：按维护者要求，已删除 worklogs 根目录下的 acceptance-2026-09-03-qwen、manager-fixes-2026-09-03 和 archive 三个历史材料目录。下文涉及这些目录的附件、备份和复现命令仅作历史记录，已不再可用；原验证结论保留。

作者（GitHub）：[@Q-Hz](https://github.com/Q-Hz)  
作者唯一标识：`github.com:151645603`（[按数字 ID 查询当前账号](https://api.github.com/user/151645603)）

## 修改与依据

按维护者要求，将既有 19 篇日志及根目录配套截图、补丁共 30 个文件移入 `worklogs/151645603/`。目录名不依赖开发者用户名或邮箱；历史作者归属的推定依据及在线核验限制继续见[作者标识规范](../README.md)。

更新日志中的规范链接、迁移文件引用，以及管理界面 PRD 中指向日志的 4 个链接。共享验收材料及 archive 目录保持原位。更新 `AGENTS.md` 与 `worklogs/README.md`，规定后续日志的目录、多人参与和未知作者处理方式。

## 追加：公开 AGENTS.md 的独立入口规则（2026-09-05）

维护者：`github.com:151645603`（@Q-Hz）。按维护者要求扩充 `AGENTS.md` 第 5 节，使 agent 无需先阅读其他文件即可确定日志路径。明确人类开发者身份解析、GitHub 数字 ID 获取、未知身份暂存、作者元信息、多人归属及改名/换邮箱/换账号的处理规则。禁止默认沿用仓库所有者或既有日志作者的 ID。

验证：检查第 5 节 Markdown 结构、路径示例及与 `worklogs/README.md` 的一致性；`git diff --check` 通过。文档修改，无需应用测试或构建。

## 追加：AGENTS.md 中文化（2026-09-05）

维护者：`github.com:151645603`（@Q-Hz）。按要求将 `AGENTS.md` 的标题与规则正文翻译为中文，保留原有 10 个章节、命令、路径、API 字段和专业名词，更新最后修改日期。开发者数字 ID 归档及身份不明时的处理规则保持不变。

验证：逐项核对翻译前后反引号内的路径、命令和字段示例，以及 10 个章节的完整性；检查 Markdown 结构。仅文档翻译，无需应用测试或构建，未调整 Git 忽略规则。

## 追加：精简公开入口与本地资料说明（2026-09-05）

维护者：`github.com:151645603`（@Q-Hz）。按要求移除 `AGENTS.md` 的固定本机路径和外部参考指南比较，将本地资料的详细使用规则移入该目录的 README。公开指南仅保留可选入口，明确公开 checkout 不应默认依赖本地资料。

检查发现当前 `.gitignore` 已取消对 worklogs 的目录忽略，因此同步修正指南中过时的“worklogs 仅本地保存”描述；未修改 `.gitignore`，也未提交文件。此前记录中的忽略状态描述属于当时状态。

验证：检查 Markdown 结构与入口路径，确认本地 README 仍被 Git 忽略；运行 `git diff --check`。纯文档修改，无需构建或应用测试。

## 原迁移检查结果

- 移动前检查绝对路径和目标重名，逐文件比较移动前后 SHA-256，确认移动未改变文件内容，再更新引用。
- 校验 19 篇迁移日志的作者 ID 与目录一致、规范链接可解析，以及 PRD 的 4 个日志链接可解析。
- 纯文档整理，不运行应用测试或构建；未改变 Git 忽略规则，未提交或发布。
- 原日志内指向本机源码的历史绝对路径不在本次迁移范围；本次不代表完成公开发布前的全文整理。
