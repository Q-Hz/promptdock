# 版本更新至 v1.7.0

作者（GitHub）：[@Q-Hz](https://github.com/Q-Hz)
作者唯一标识：`github.com:151645603`（[按数字 ID 查询当前账号](https://api.github.com/user/151645603)）

## 摘要与修改动机

按用户要求，在文件夹管理、首次使用导览、设置界面重构、深色主题统一和 WebView 入口限制等功能完成后，将 PromptDock 应用版本统一更新为 1.7.0。

## 受影响区域

- `package.json`、`package-lock.json`：通过 npm 版本命令同步应用版本。
- `src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`：同步 Rust 包版本。
- `src-tauri/tauri.conf.json`：同步 Tauri 应用版本。
- `README.md`、`README_CN.md`：同步中英文版本徽章。

## 重要实现决策

- 本次仅更新应用自身版本，不升级第三方依赖。
- 不创建 Git tag，不构建安装包，也不发布 Release。
- lockfile 由对应包管理工具更新，不手工改写依赖解析结果。

## 验证

- `npm test`：通过，73 项测试全部成功。
- `npm run build`：通过，Vue TypeScript 检查与 Vite 生产构建成功。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：通过。
- `cargo check --offline --manifest-path src-tauri/Cargo.toml`：通过，编译版本显示 1.7.0。
- `cargo test --offline --manifest-path src-tauri/Cargo.toml`：通过，66 项测试全部成功。
- `git diff --check`：通过。

## 剩余风险、限制或后续工作

- 本次不进行安装包、自动更新服务器或真实原生窗口验收。
