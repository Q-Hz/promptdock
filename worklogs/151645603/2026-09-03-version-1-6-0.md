# 版本更新至 v1.6.0

作者（GitHub）：[@Q-Hz](https://github.com/Q-Hz)  
作者唯一标识：`github.com:151645603`（[按数字 ID 查询当前账号](https://api.github.com/user/151645603)）  
归属依据：历史日志按本地相关提交作者补录，说明见 [作者标识规范](../README.md)。

## 修改范围

按用户要求，在已有管理界面、排序与置顶功能的未提交改动上统一应用版本为 1.6.0，并提供对应的提交描述。

- `package.json`、`package-lock.json`：执行 `npm version 1.6.0 --no-git-tag-version --ignore-scripts` 更新。
- `src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`：应用版本更新为 1.6.0。
- `src-tauri/Cargo.lock`：通过 Cargo 离线检查自动更新本地 promptdock 包版本。
- `README.md`、`README_CN.md`：版本徽章同步更新。

锁文件差异仅涉及应用自身版本，没有更新第三方依赖。保留所有既有未提交修改；未创建 Git commit/tag，未生成安装包或发布 Release。

## 验证

- `npm test`：65/65 通过。
- `npm run build`：通过。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：通过。
- `cargo check --offline --manifest-path src-tauri/Cargo.toml`：通过，编译版本显示 1.6.0。
- `cargo test --offline --manifest-path src-tauri/Cargo.toml`：57/57 通过。
- 已审查版本相关文件差异，确认 npm、Rust、Tauri 和 README 版本一致。

本次未进行安装包、更新服务器或原生窗口验收。
